## Packages
recharts | Dashboard charts for admin and owner stats
framer-motion | Smooth transitions and page animations
lucide-react | Beautiful icons for the UI
react-hook-form | Form handling
@hookform/resolvers | Zod resolvers for forms

## Notes
Authentication uses session-based auth with credentials: "include"
Admin dashboard requires aggregations
Owner dashboard requires filtering ratings by store
